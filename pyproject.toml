[project]
name = "mcp-transfer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.7.1",
    "paramiko>=3.5.1",
    "pyyaml>=6.0.2",
]
